# Server Configuration
PORT=3001
NODE_ENV=development

# MongoDB Configuration
# For Local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/lynq

# For MongoDB Atlas (Cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/lynq?retryWrites=true&w=majority

# Your Atlas URI from MongoDB Compass:
MONGODB_URI=your_mongodb_atlas_uri_here

# Arc Network
ARC_TESTNET_RPC_URL=https://rpc.testnet.arc.network
CHAIN_ID=5042002

# Agent Wallet (Backend acts as verifier and screening agent)
# This wallet needs to have VERIFIER_ROLE in VerificationSBT contract
AGENT_PRIVATE_KEY=your_private_key_without_0x_prefix

# JWT Secret (optional for future auth)
JWT_SECRET=your_jwt_secret_here

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Screening Thresholds
MIN_CREDIT_SCORE_TIER1=700
MIN_CREDIT_SCORE_TIER2=600
MIN_CREDIT_SCORE_TIER3=500
BASE_INTEREST_RATE=500
MAX_LOAN_AMOUNT=50000

# CORS
CORS_ORIGIN=http://localhost:3000

